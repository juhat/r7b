<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<p><%= t('.magic_login_instruction') %></p>

<p><%= link_to t('.magic_login_link_text'), send("#{@scope_name.to_s.pluralize}_magic_link_url", Hash[@scope_name, {email: @resource.email, token: @token, remember_me: @remember_me}]) %></p>

<p><%= t('.expirity_message') %> <%= Devise.passwordless_login_within.inspect %>.</p>

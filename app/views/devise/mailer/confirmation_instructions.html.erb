<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<p><%= t('.instruction') %></p>

<p><%= link_to ('.link_text'), confirmation_url(@resource, confirmation_token: @token) %></p>

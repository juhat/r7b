<h2 class="mb-4"><%= t(".title") %></h2>
<%= turbo_frame_tag "announcements", src: announcements_path(items: 2) do %>
  <div class="spinner-border" role="status">
    <span class="visually-hidden">Loading...</span>
  </div>
<% end %>

<%= turbo_frame_tag @announcement do %>
  <%= render @announcement %>

  <div>
    <%= link_to "Edit", edit_announcement_path(@announcement), class: "btn btn-primary" %>
    <%= button_to "Destroy this announcement", @announcement, method: :delete, class: "btn btn-danger" %>
  </div>
<% end %>
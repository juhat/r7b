<turbo-frame id="announcements">
  <%= turbo_stream_from "announcements" %>
  <% @announcements.each do |announcement| %>
    <%= render announcement %>
  <% end %>
</turbo-frame>

<%== pagy_bootstrap_nav(@pagy) %>

<%= link_to "New announcement", new_announcement_path %>
